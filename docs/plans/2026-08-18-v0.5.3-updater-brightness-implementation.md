# MacRemote v0.5.3 Updater and Brightness Implementation Plan

1. Add failing JVM tests for saved-display target resolution and brightness response decoding.
2. Implement the target resolver and typed brightness result codec; wire them into display loading and brightness commands.
3. Add failing JVM tests for update presentation transitions.
4. Implement explicit update-check state and a reusable update action panel shown on Devices and Remote screens.
5. Add or tighten server/API regression tests for brightness semantic failures.
6. Run focused tests after each change, then the complete Android, server, extension, lint, build, signing, and upgrade checks.
7. Run an adversarial Claude review of the complete uncommitted diff, resolve confirmed findings test-first, and rerun all gates.
8. Tag and publish v0.5.3, then verify the public APK and update discovery from the prior release.
