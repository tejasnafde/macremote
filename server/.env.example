# Copy to .env and fill in real values. NEVER commit .env.
APP_ENV=dev
API_TOKEN=change-me-to-a-long-random-token
DISCORD_WEBHOOK_URL=
PORT=8484

# Optional overrides (sane defaults baked into config/settings.py)
# HS_BIN=/opt/homebrew/bin/hs
# VOLUME_STEP=6
# LOG_DIR=~/Library/Logs/macremote
